import { useState } from "react"
import type { FormEvent } from "react"
import { BookOpen, LayoutDashboard, ClipboardList, Award, Search, LogOut, User, Sparkles } from "lucide-react"
import { useNavigate } from "react-router-dom"
import { API } from "../config"
type Result={title:string;content:string;type:string;course_id:number;distance:number}
export default function AISearch(){
 const navigate=useNavigate(),[query,setQuery]=useState(""),[results,setResults]=useState<Result[]>([]),[loading,setLoading]=useState(false),[error,setError]=useState("")
 const role=localStorage.getItem("role")||"student",name=localStorage.getItem("name")||"User"
 async function search(e?:FormEvent){e?.preventDefault();const token=localStorage.getItem("token");if(!token){navigate("/login");return}if(!query.trim())return;setLoading(true);setError("");try{const res=await fetch(`${API}/ai-search?q=${encodeURIComponent(query)}`,{headers:{Authorization:`Bearer ${token}`}}),data=await res.json();if(!res.ok){if(res.status===404)throw new Error("AI Search is not available on the deployed backend yet. Redeploy the latest LMS backend, then try again.");throw new Error(data.detail||"Search failed")};setResults(data.results)}catch(e){setError(e instanceof Error?e.message:"Search failed")}finally{setLoading(false)}}
 function logout(){localStorage.clear();navigate("/login")}
 return <div className="min-h-screen bg-[#070b14] text-white"><aside className="fixed left-0 top-0 hidden h-screen w-64 border-r border-white/10 bg-[#0b101a] p-5 lg:block"><div className="flex items-center gap-3 px-3 py-4"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-white text-black"><BookOpen size={21}/></div><div><h1 className="font-semibold">LMS</h1><p className="text-xs text-white/40">Learning Platform</p></div></div><nav className="mt-8 space-y-2"><button onClick={()=>navigate(role==="teacher"?"/teacher":role==="admin"?"/admin":"/dashboard")} className="flex w-full items-center gap-3 rounded-xl px-4 py-3 text-sm text-white/50 hover:bg-white/5 hover:text-white"><LayoutDashboard size={18}/>Dashboard</button><button onClick={()=>navigate("/courses")} className="flex w-full items-center gap-3 rounded-xl px-4 py-3 text-sm text-white/50 hover:bg-white/5 hover:text-white"><BookOpen size={18}/>My Courses</button><button onClick={()=>navigate("/assignments")} className="flex w-full items-center gap-3 rounded-xl px-4 py-3 text-sm text-white/50 hover:bg-white/5 hover:text-white"><ClipboardList size={18}/>Assignments</button><button onClick={()=>navigate("/marks")} className="flex w-full items-center gap-3 rounded-xl px-4 py-3 text-sm text-white/50 hover:bg-white/5 hover:text-white"><Award size={18}/>Marks</button><button className="flex w-full items-center gap-3 rounded-xl bg-white/10 px-4 py-3 text-sm"><Search size={18}/>AI Search</button></nav><button onClick={logout} className="absolute bottom-6 left-5 right-5 flex items-center gap-3 rounded-xl px-4 py-3 text-sm text-white/50 hover:bg-white/5 hover:text-white"><LogOut size={18}/>Logout</button></aside><main className="lg:ml-64"><header className="flex items-center justify-between border-b border-white/10 px-6 py-5 lg:px-10"><div><p className="text-sm text-white/40">AI-Powered Smart Search</p><h2 className="mt-1 text-2xl font-semibold">Find learning material</h2></div><div className="flex items-center gap-3"><span className="hidden text-sm text-white/40 sm:block">{name}</span><div className="flex h-10 w-10 items-center justify-center rounded-full border border-white/10 bg-white/5"><User size={18}/></div></div></header><section className="p-6 lg:p-10"><div className="mx-auto max-w-4xl"><div className="mb-8 rounded-3xl border border-white/10 bg-white/[0.035] p-6"><div className="flex items-center gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-white text-black"><Sparkles size={20}/></div><div><h3 className="font-semibold">Semantic Search</h3><p className="text-sm text-white/40">Search courses and learning resources by meaning, not just exact words.</p></div></div><form onSubmit={search} className="mt-6 flex flex-col gap-3 sm:flex-row"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="e.g. explain database normalization" className="flex-1 rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-sm outline-none placeholder:text-white/20 focus:border-white/30"/><button disabled={loading} className="flex items-center justify-center gap-2 rounded-xl bg-white px-6 py-3 text-sm font-medium text-black hover:bg-white/90 disabled:opacity-50"><Search size={17}/>{loading?"Searching...":"Search"}</button></form>{error&&<div className="mt-4 rounded-xl border border-red-400/20 bg-red-400/10 px-4 py-3 text-sm text-red-300">{error}</div>}</div>{results.length>0&&<div className="space-y-4"><p className="text-sm text-white/40">Relevant results</p>{results.map((r,i)=><div key={i} className="rounded-2xl border border-white/10 bg-white/[0.035] p-6"><div className="flex items-start justify-between gap-4"><div><span className="text-xs uppercase tracking-wider text-white/30">{r.type}</span><h3 className="mt-2 text-lg font-medium">{r.title}</h3></div><span className="rounded-full bg-white/10 px-3 py-1 text-xs text-white/50">Course {r.course_id}</span></div><p className="mt-3 text-sm leading-6 text-white/50">{r.content}</p></div>)}</div>}{!loading&&!error&&results.length===0&&<div className="rounded-2xl border border-white/10 bg-white/[0.03] p-10 text-center text-white/40"><Search className="mx-auto mb-4" size={34}/>Enter a query to find relevant learning material.</div>}</div></section></main></div>
}
